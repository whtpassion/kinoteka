# 🏗️ Диаграмма компонентов проекта "Кинотека"
![Диаграмма components](screenshots/d_components.png?raw=true "d_components")
## 📋 Обзор архитектуры

Данная диаграмма компонентов представляет высокоуровневую архитектуру веб-сервиса "Кинотека", демонстрируя взаимодействие между основными модулями системы.

## 🎯 Компоненты системы

### 🖥️ Frontend-компоненты

**Веб-клиент (`ВебКлиент`)**
- **Назначение**: Пользовательский интерфейс для браузеров
- **Основные функции**:
  - `ГлавнаяСтраница()` - лендинг и навигация
  - `ПоискФильмов()` - интерфейс поиска и фильтрации
  - `ПрофильПользователя()` - управление учетной записью
  - `УправлениеПодборками()` - создание и редактирование коллекций

### ⚙️ Backend-компоненты

**API Gateway (`БэкендAPI`)**
- **Назначение**: Единая точка входа для всех клиентов
- **Контроллеры**:
  - `AuthController()` - аутентификация и авторизация
  - `FilmController()` - управление фильмами
  - `UserController()` - операции с пользователями
  - `CollectionController()` - работа с подборками

**Сервисный слой (`СервисныйСлой`)**
- **Назначение**: Реализация бизнес-логики
- **Сервисы**:
  - `FilmService()` - логика работы с фильмами
  - `UserService()` - управление пользователями
  - `CollectionService()` - бизнес-логика подборок
  - `ReviewService()` - обработка обзоров и комментариев
  - `SearchService()` - продвинутый поиск и фильтрация

### 💾 Data-компоненты

**База данных (`БазаДанных`)**
- **Назначение**: Основное хранилище данных
- **Основные сущности**:
  - `Фильмы` - каталог фильмов с метаданными
  - `Пользователи` - учетные записи и профили
  - `Подборки` - пользовательские коллекции
  - `Комментарии` - система обсуждений
  - `Оценки` - рейтинговая система

**Внешние сервисы (`ВнешниеСервисы`)**
- **Назначение**: Интеграции с внешними системами
- **Сервисы**:
  - `TMDB API` - получение актуальных данных о фильмах
  - `Cloud Storage` - хранение медиафайлов
  - `Email Service` - уведомления и рассылки

## 🔗 Взаимодействие компонентов

### Протоколы связи:
- **Frontend → Backend**: HTTP/REST API
- **Backend → Data**: SQL-запросы (база данных), API вызовы (внешние сервисы)
- **Внутри Backend**: Вызовы методов между слоями

### Потоки данных:
1. **Поиск фильма**: ВебКлиент → БэкендAPI → SearchService → БазаДанных
2. **Создание подборки**: ВебКлиент → БэкендAPI → CollectionService → БазаДанных
3. **Добавление комментария**: ВебКлиент → БэкендAPI → ReviewService → БазаДанных

## 🛠 Технологический стек

| Компонент | Возможные технологии |
|-----------|---------------------|
| **ВебКлиент** | React, Vue.js, Angular |
| **БэкендAPI** | Node.js/Express, Spring Boot, Django |
| **БазаДанных** | PostgreSQL, MySQL, MongoDB |
| **ВнешниеСервисы** | The Movie Database API, AWS S3, SendGrid |

## 📈 Масштабируемость

Архитектура спроектирована с учетом горизонтального масштабирования:
- Статический фронтенд может размещаться на CDN
- Микросервисная структура бэкенда позволяет масштабировать отдельные компоненты
- База данных поддерживает репликацию и шардирование
