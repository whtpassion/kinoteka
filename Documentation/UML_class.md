# 📊 UML Диаграмма классов проекта "Кинотека"

![Диаграмма uml_class](screenshots/uml_class.png?raw=true "uml_class")

## 🎯 Обзор архитектуры данных

Данная диаграмма представляет объектную модель данных веб-сервиса "Кинотека", отображая ключевые сущности, их атрибуты, методы и взаимосвязи между ними.

## 🏗️ Основные сущности системы

### 🎬 Фильм (Film)
**Центральная сущность системы**, содержащая информацию о кинопроизведениях.

| Атрибут | Тип | Описание |
|---------|-----|----------|
| `id` | UUID | Уникальный идентификатор |
| `title` | String | Название фильма на русском |
| `originalTitle` | String | Оригинальное название |
| `description` | String | Описание фильма |
| `releaseYear` | Integer | Год выпуска |
| `country` | String | Страна производства |
| `duration` | Integer | Продолжительность в минутах |
| `averageRating` | Float | Средний рейтинг (вычисляемый) |

**Ключевые методы:**
- `addActor()` - добавление актера
- `addGenre()` - добавление жанра
- `addTag()` - добавление тега
- `calculateAverageRating()` - расчет среднего рейтинга

### 👤 Пользователь (User)
**Актор системы**, представляющий зарегистрированного пользователя.

| Атрибут | Тип | Описание |
|---------|-----|----------|
| `id` | UUID | Уникальный идентификатор |
| `username` | String | Имя пользователя |
| `email` | String | Email (уникальный) |
| `passwordHash` | String | Хэш пароля |
| `registrationDate` | DateTime | Дата регистрации |
| `role` | Role | Роль пользователя |

**Ключевые методы:**
- `createCollection()` - создание подборки
- `writeReview()` - написание обзора
- `addComment()` - добавление комментария
- `rateFilm()` - оценка фильма

### 📚 Подборка (Collection)
**Пользовательская коллекция фильмов**, основной инструмент организации контента.

| Атрибут | Тип | Описание |
|---------|-----|----------|
| `id` | UUID | Уникальный идентификатор |
| `title` | String | Название подборки |
| `description` | String | Описание подборки |
| `isPublic` | Boolean | Публичный доступ |
| `createdAt` | DateTime | Дата создания |
| `author` | User | Автор подборки |

**Ключевые методы:**
- `addFilm()` - добавление фильма
- `removeFilm()` - удаление фильма

### 💬 Контент пользователей

#### Обзор (Review)
**Развернутый отзыв** о фильме с заголовком и структурированным содержанием.

| Атрибут | Тип | Описание |
|---------|-----|----------|
| `id` | UUID | Уникальный идентификатор |
| `title` | String | Заголовок обзора |
| `content` | String | Содержание обзора |
| `createdAt` | DateTime | Дата создания |
| `updatedAt` | DateTime | Дата обновления |

**Ключевые методы:**
- `updateContent()` - обновление содержимого

#### Комментарий (Comment)
**Краткое сообщение** в обсуждении фильма.

| Атрибут | Тип | Описание |
|---------|-----|----------|
| `id` | UUID | Уникальный идентификатор |
| `content` | String | Текст комментария |
| `createdAt` | DateTime | Дата создания |

#### Оценка (Rating)
**Числовая оценка** фильма пользователем.

| Атрибут | Тип | Описание |
|---------|-----|----------|
| `id` | UUID | Уникальный идентификатор |
| `score` | Integer | Оценка (1-10) |
| `ratedAt` | DateTime | Дата оценки |

### 🏷️ Категоризация

#### Жанр (Genre)
**Предопределенная категория** фильмов.

| Атрибут | Тип | Описание |
|---------|-----|----------|
| `id` | UUID | Уникальный идентификатор |
| `name` | String | Название жанра |
| `description` | String | Описание жанра |

#### Тег (Tag)
**Пользовательская метка** для гибкой категоризации.

| Атрибут | Тип | Описание |
|---------|-----|----------|
| `id` | UUID | Уникальный идентификатор |
| `name` | String | Название тега |
| `createdBy` | User | Создатель тега |
| `createdAt` | DateTime | Дата создания |

#### Человек (Person)
**Участник кинопроизводства** (актер, режиссер).

| Атрибут | Тип | Описание |
|---------|-----|----------|
| `id` | UUID | Уникальный идентификатор |
| `fullName` | String | Полное имя |
| `dateOfBirth` | Date | Дата рождения |
| `biography` | String | Биография |

### 🎭 Роль (Role)
**Перечисление ролей** пользователей для управления правами.

## 🔗 Взаимосвязи между сущностями

### Отношения "один-ко-многим"
- **Пользователь** → **Подборка** (1:*)
- **Пользователь** → **Обзор** (1:*)
- **Пользователь** → **Комментарий** (1:*)
- **Пользователь** → **Оценка** (1:*)
- **Фильм** → **Обзор** (1:*)
- **Фильм** → **Комментарий** (1:*)
- **Фильм** → **Оценка** (1:*)

### Отношения "многие-ко-многим"
- **Фильм** ↔ **Жанр** (*:*)
- **Фильм** ↔ **Человек** (актеры) (*:*)
- **Фильм** ↔ **Тег** (*:*)
- **Фильм** ↔ **Подборка** (*:*)

### Отношения "один-к-одному"
- **Фильм** → **Человек** (режиссер) (1:1)

## 🗄️ Схема базы данных

### Основные таблицы:
1. `films` - фильмы
2. `users` - пользователи
3. `collections` - подборки
4. `reviews` - обзоры
5. `comments` - комментарии
6. `ratings` - оценки

### Связующие таблицы:
- `film_genres` - связь фильмов и жанров
- `film_actors` - связь фильмов и актеров
- `collection_films` - связь подборок и фильмов
- `film_tags` - связь фильмов и тегов

## 🔧 Технические особенности

### Паттерны проектирования:
- **Active Record** - для сущностей с бизнес-логикой
- **Repository** - для доступа к данным
- **Observer** - для обновления вычисляемых полей

### Оптимизации:
- **Индексы** на часто используемых полях
- **Кэширование** среднего рейтинга фильмов
- **Денормализация** для часто запрашиваемых данных

### Валидации:
- Уникальность email пользователя
- Ограничение оценки (1-10)
- Проверка даты выпуска фильма

## 📈 Масштабируемость

### Горизонтальное масштабирование:
- Шардирование по пользователям
- Репликация для чтения
- Кэширование на уровне приложения

### Вертикальное масштабирование:
- Выделение горячих таблиц
- Оптимизация индексов
- Архивация исторических данных
