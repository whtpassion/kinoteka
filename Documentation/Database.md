# Диаграмма базы данных
![Диаграмма базы данных](screenshots/DB_diagramm.png?raw=true "DB")

## 🏷️ Структура диаграммы

- Таблица Users - таблица с пользователями (primary key - ID_user)
    1. ID_user - уникальный номер пользователя (тип данных integer)
    2. Username - имя пользователя (тип данных text)
    3. Password - пароль пользователя (тип данных text)

      
- Таблица Films - таблица с фильмами (primary key - Name)
    1. Name - название фильма (тип данных text)
    2. Date - дата выхода во всемирный прокат (тип данных date)
    3. Length - длина фильма в минутах (тип данных integer)
    4. Country - страна производства (тип данных text)
    5. Description - описание фильма (тип данных text)

       
- Таблица Scores - таблица с отзывами (primary key - ID_score, foreign keys - Film, Author)
    1. ID_score - уникальный номер отзыва (тип данных text)
    2. Score_text - текст отзыва (тип данных text)
    3. Score - оценка фильма (тип данных integer)
    4. Score_date - дата написания отзыва (тип данных date)
    5. Film - название фильма (тип данных text)
    6. Author - никнейм автора отзыва (тип данных text)
